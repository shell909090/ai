[project]
name = "hitl-bridge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0.0"]

[project.scripts]
hitl-bridge = "hitl_bridge:main"

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
